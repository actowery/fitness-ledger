[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fitness-ledger"
version = "0.4.0"
description = "Library-native conversational nutrition and fitness data skills"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = []
